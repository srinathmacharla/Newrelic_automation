---
newrelic:
  - account: 4339676
    tags:
      environment: preprod
      workspace: default

services:
  - name: Example_dashboard
    description: null
    permissions: PUBLIC_READ_WRITE
    components:
      - AWS ECS
      - HTTP Flows


